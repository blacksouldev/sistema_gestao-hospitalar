import 'package:flutter/material.dart';

class ProfissionaisPage extends StatelessWidget {
  const ProfissionaisPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Profissionais')),
      body: const Center(child: Text('Página de Profissionais')),
    );
  }
}
